import datetime; etaHours = 0; etaMinutes = 0; etaSeconds = 0; etaFramecnt = 0; etastring = (datetime.datetime.now() + (datetime.timedelta(hours=etaHours, minutes=etaMinutes, seconds=etaSeconds)*etaFramecnt)); print(etastring.strftime("Current rendering is estimated to complete on %A, %B %d %Y, at %I:%M:%S %p"));
